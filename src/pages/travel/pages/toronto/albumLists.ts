import { photoItem } from '../../../../util/types';

export interface torontoPlaces {
  toronto: photoItem[];
}

const toronto: photoItem[] = [];

export const torontoList: torontoPlaces = {
  toronto: toronto,
};
