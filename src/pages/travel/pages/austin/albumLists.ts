import { photoItem } from '../../../../util/types';

export interface austinPlaces {
  austin: photoItem[];
}

const austin: photoItem[] = [];

export const austinList: austinPlaces = {
  austin: austin,
};
